<aside class="sidebar">
    <!-- Logo Section -->
    <div class="logo-section">
        <img src="https://assets.ccbp.in/frontend/react-js/planner-app-logo-img.png" alt="app logo" class="logo" />
    </div>

    <!-- Plan Section -->
    <div class="plan-section">
        <h2>Plan</h2>
        <p>Manage all your plans in one place!</p>
    </div>

    <!-- Categories Section -->
      <div class="categories-section">
        <h2>Categories</h2>
        <ul class="categories-list">
            <li><a href="/events">Events</a></li>
            <li><a href="/vacations">Vacations</a></li>
            <li><a href="/careers">Careers</a></li>
            <li><a href="/shopping">Shopping List</a></li>
        </ul>
    </div>

    <!-- Help Section -->
    <div class="help-section">
        <button id="helpButton">Get Help to Plan</button>
    </div>
</aside>

<style>
     /* General styling */
  body {
    margin: 0;
    padding: 0;
    font-family: Arial, sans-serif;
    background-color: #FEFBF6;
  }
   /* Sidebar styling */
  .sidebar {
    position: fixed; /* Fix the sidebar to the left of the screen */
    top: 0;
    left: 0;
    width: 250px;
    height: 100vh;
    background-color: #29231D;
    color: #FEFBF6;
    padding: 20px;
    box-sizing: border-box;
    overflow-y: auto; /* Allow scrolling if the content is long */
  }


    /* Logo styling */
    .logo-section {
        text-align: center;
        margin-bottom: 20px;
    }

    .logo {
        max-width: 150px;
        height: auto;
    }

    /* Plan section styling */
    .plan-section {
        margin-bottom: 30px;
    }

    .plan-section h2 {
        font-size: 20px;
        margin-bottom: 10px;
        color: #FEFBF6;
    }

    .plan-section p {
        color: #A3A3A3;
        font-size: 14px;
    }

    /* Categories section styling */
    .categories-section {
        margin-bottom: 30px;
    }

    .categories-section h2 {
        font-size: 20px;
        margin-bottom: 10px;
        color: #FEFBF6;
    }

    .categories-section ul {
        list-style-type: none;
        padding: 0;
    }

    .categories-section ul li {
        margin-bottom: 10px;
    }

    .categories-section ul li a {
        color: #FEFBF6;
        text-decoration: none;
        font-size: 16px;
        padding: 10px;
        display: block;
        border-radius: 5px;
        transition: background-color 0.3s;
    }

    .categories-section ul li a:hover {
        background-color: #FEFBF6;
        color: #29231D;
    }

    /* Help section styling */
    .help-section {
        text-align: center;
        margin-top: auto;
    }

    #helpButton {
        background-color: #FEFBF6;
        color: #29231D;
        border: none;
        padding: 10px 20px;
        font-size: 16px;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s;
    }

    #helpButton:hover {
        background-color: #97AFAF;
        color: #FEFBF6;
    }
     /* Responsive Styles */
    @media screen and (max-width: 1024px) {
        .sidebar {
            width: 200px;
            padding: 15px;
        }
        
        .logo {
            max-width: 120px;
        }

        .categories-section ul li a {
            font-size: 14px;
        }

        #helpButton {
            font-size: 14px;
            padding: 8px 16px;
        }
    }

    @media screen and (max-width: 768px) {
        /* Collapsible sidebar for tablets and smaller screens */
        .sidebar {
            width: 180px;
        }

        .logo {
            max-width: 100px;
        }

        .categories-section ul li a {
            font-size: 13px;
        }

        #helpButton {
            font-size: 12px;
            padding: 6px 12px;
        }
    }

    @media screen and (max-width: 576px) {
        /* Hide sidebar on mobile and show a toggle button */
        .sidebar {
            width: 0;
            overflow: hidden;
        }

        /* Show a menu button for mobile users */
        .menu-toggle {
            display: block;
            position: fixed;
            top: 20px;
            left: 20px;
            background-color: #29231D;
            color: #FEFBF6;
            padding: 10px;
            cursor: pointer;
            font-size: 18px;
            z-index: 1000;
            border-radius: 5px;
        }

        /* When the sidebar is opened */
        .sidebar.open {
            width: 180px;
        }
    }
  /* Main content styling */
  .main-content {
    margin-left: 270px; /* Push the content to the right to make space for the sidebar */
    padding: 20px;
    box-sizing: border-box;
    background-color: #FEFBF6;
    min-height: 100vh; /* Make sure the main content takes the full height of the screen */
  }

  /* Styling for the lists of careers, vacations, etc. */
  h1, h2 {
    color: #29231D;
  }

  ul {
    list-style-type: none;
    padding: 0;
  }

  ul li {
    background-color: #97AFAF;
    margin: 10px 0;
    padding: 10px;
    border-radius: 5px;
    color: #29231D;
  }

  /* Responsive Design: Adjust layout for smaller screens */
  @media screen and (max-width: 768px) {
    .sidebar {
      width: 200px; /* Narrower sidebar on smaller screens */
    }

    .main-content {
      margin-left: 220px; /* Adjust main content margin */
    }
  }

  @media screen and (max-width: 576px) {
    .sidebar {
      width: 100%; /* Make sidebar full width on very small screens */
      height: auto;
      position: relative;
    }

    .main-content {
      margin-left: 0; /* No margin for the main content on small screens */
    }
  }
    
</style>

<script>
    document.addEventListener('DOMContentLoaded', () => {
    const menuToggle = document.createElement('div');
    menuToggle.className = 'menu-toggle';
    menuToggle.innerHTML = '&#9776;'; // Hamburger menu icon
    document.body.appendChild(menuToggle);

    const sidebar = document.querySelector('.sidebar');
    menuToggle.addEventListener('click', () => {
        sidebar.classList.toggle('open');
    });

    document.addEventListener('DOMContentLoaded', () => {
    const helpButton = document.getElementById('helpButton');
    helpButton.addEventListener('click', () => {
        alert('Redirecting to the help page...');
        // Example of redirection to a help page
        window.location.href = '/help';
    });
});

});
</script>

